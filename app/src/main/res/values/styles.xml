<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:actionButtonStyle">@style/ActionButton</item>
    </style>

    <style name="FullScreenDark" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">@color/black</item>
    </style>

    <style name="fullScreenLight" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">@color/white</item>
    </style>

    <!-- TODo is this style really necessary? the sidebar can get the darkActionBarDirectly -->
    <style name="toolbar" parent="@style/ThemeOverlay.AppCompat.Dark.ActionBar">
        <!-- used for the title of the Toolbar - as long as not overridden -->
        <item name="android:textColorPrimary">#d1afa1</item>
        <!-- Used for color back button and three dots -->
        <item name="android:background">?colorPrimary</item>
    </style>


    <style name="ActionButton" parent="@android:style/Widget.ActionButton">
        <item name="android:drawablePadding">5dp</item>
        <item name="android:minWidth">0dip</item>
    </style>




</resources>
